# see https://hub.docker.com/_/gcc/
image: gcc

build:
  stage: build
  before_script:
    - apt update && apt -y install make automake libtool libtool-bin autoconf doxygen gfortran g++ 
  script:
    - ./non_interactive_autogen.sh -s

test:
  stage: test
  script:
    - make check -k -s
